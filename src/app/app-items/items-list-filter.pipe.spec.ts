import { ItemsListFilterPipe } from './items-list-filter.pipe';

describe('ItemsListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
